# Quick Start Guide

## 1. Install Dependencies

```bash
pnpm install
```

## 2. Create Placeholder Assets

Create these files in `assets/` (or use placeholders):
- `icon.png` (1024x1024)
- `splash.png` (1284x2778)
- `adaptive-icon.png` (1024x1024)
- `favicon.png` (48x48)

## 3. Start the App

```bash
pnpm start
```

Then press:
- `i` for iOS simulator
- `a` for Android emulator
- `w` for web

## 4. Get GitHub Token

1. Visit https://github.com/settings/tokens
2. Generate new token (classic)
3. Select scopes: `repo`, `read:user`, `user:email`
4. Copy the token (starts with `ghp_`)
5. Use it in the app's sign-in screen

## That's it! 🎉

You're ready to use Octo!
